# EDYC Website Starter

This repository contains a starter React site inspired by edyc.co.uk.

To run locally:
1. Install dependencies: `npm install`
2. Start: `npm start`

This was added by an automated assistant.